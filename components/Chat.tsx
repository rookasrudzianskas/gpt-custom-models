import React from 'react';

type Props = {
  chatId: string
}

const Chat = ({chatId}: Props) => {
  return (
    <div className="flex flex-1">
      Chat
    </div>
  );
};

export default Chat;
// by Rokas with ❤️
