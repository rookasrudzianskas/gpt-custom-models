import React from 'react';

type Props = {
  chatId: string
}

const ChatInput = ({chatId}: Props) => {
  return (
    <div>
      CjatInput
    </div>
  );
};

export default ChatInput;
// by Rokas with ❤️
